import { useState } from 'react'

import styles from './homepage.module.css';

export const Homepage = () => {

  return (
    <div className={styles.homepage}>
      <p className={styles.text}>Homepage</p>
    </div>
  )
}
